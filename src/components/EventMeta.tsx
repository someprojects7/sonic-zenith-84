/** Small red flag for events added since the last visit. */
export function NewBadge() {
  return (
    <span className="inline-flex h-4 shrink-0 items-center rounded-full bg-rausch px-1.5 text-[12px] font-semibold uppercase leading-none tracking-[0.06em] text-white">
      New
    </span>
  );
}
